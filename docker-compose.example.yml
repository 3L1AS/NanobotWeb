services:
  # The sleek local dashboard
  nanobot-dashboard:
    container_name: nanobot-dashboard
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      # Next API routes look for os.homedir() + .nanobot which is /root/.nanobot in our alpine image
      - ~/.nanobot:/root/.nanobot
      # Essential: Mount the host's Docker socket so NanoboardWeb can run `docker logs` and `docker restart`
      - /var/run/docker.sock:/var/run/docker.sock
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - DASHBOARD_PASSWORD=admin # Change this in production
      - NANOBOT_CONTAINER_NAME=nanobot-gateway
      - NANOBOT_DIR=/root/.nanobot
